<Window x:Class="VichoRISC.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VichoRISC"
        mc:Ignorable="d"
        Title="Vicho's RISC Interpreter" Height="720" MinHeight="720" Width="1280" MinWidth="1280" Icon="cpu.ico" WindowStyle="ThreeDBorderWindow"
		xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="14"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}">
	<DockPanel>
		<Menu Style="{DynamicResource MaterialDesignMenu}" DockPanel.Dock="Top">
			<MenuItem Style="{DynamicResource MaterialDesignMenuItem}" Header="Archivo">
				<MenuItem Style="{DynamicResource MaterialDesignMenuItem}" Header="Nuevo"></MenuItem>
				<MenuItem Style="{DynamicResource MaterialDesignMenuItem}" Header="Abrir"></MenuItem>
				<MenuItem Style="{DynamicResource MaterialDesignMenuItem}" Header="Guardar"></MenuItem>
				<MenuItem Style="{DynamicResource MaterialDesignMenuItem}" Header="Guardar Como"></MenuItem>
				<MenuItem Style="{DynamicResource MaterialDesignMenuItem}" Header="Salir"></MenuItem>
			</MenuItem>
			<MenuItem Style="{DynamicResource MaterialDesignMenuItem}" Header="Depuración">
				<MenuItem Style="{DynamicResource MaterialDesignMenuItem}" Header="Ejecutar"></MenuItem>
				<MenuItem Style="{DynamicResource MaterialDesignMenuItem}" Header="Paso a paso"></MenuItem>
			</MenuItem>
		</Menu>
		<DataGrid Style="{DynamicResource MaterialDesignDataGrid}" DockPanel.Dock="Left"></DataGrid>
		<RichTextBox Style="{DynamicResource MaterialDesignRichTextBox}"></RichTextBox>
	</DockPanel>
</Window>
